---
name: update-faq
description: FAQ 추가 및 수정 시 문체의 일관성을 유지하고, 프론트/백엔드 데이터 동기화를 보장하는 패턴 가이드
---

# FAQ 업데이트 가이드 (update-faq)

이 스킬은 "자주 하는 질문(FAQ)에 ~를 추가해 줘", "FAQ 내용을 바꿔줘"와 같은 요청을 수행할 때 반드시 참고해야 하는 지침입니다.

## 1. 문체 및 작성 규칙

FAQ는 사용자가 직접 읽는 안내서이므로 친절하고 전문적인 어조를 유지해야 합니다.

- **기본 톤앤매너**: "해요체"와 "합쇼체(다나까)"를 적절히 혼용하되, 지나치게 딱딱하지 않게 작성합니다. (예: "~입니다", "~하세요", "네! ~가능합니다.")
- **시각적 요소 사용**: 설명이 복잡한 경우 가독성을 위해 이모지를 적극 활용합니다.
  - 긍정/강조: `네!`, `💡`
  - 절차/순서: `①`, `②`, `③`
  - 주의사항: `⚠️`, `💡` (참조)
- **응답 구조**: `answer` 필드는 문자열 배열(`string[]`) 형태로 작성하며, 한 문장 혹은 한 단락(절차 등)이 끝날 때마다 새로운 배열 요소로 분리하여 코드의 가독성과 프론트엔드 줄바꿈 처리를 돕습니다.

## 2. 단일 정본 규칙 (매우 중요)

**`shared/faqData.ts`가 유일한 정본(Single Source of Truth)이다. 이 파일만 수정한다.**

- `src/lib/faqData.ts`(프론트엔드)와 `functions/src/utils/faqData.ts`(백엔드)는 정본을 re-export하는 6줄짜리 껍데기다. **여기에 데이터를 직접 넣으면 정본이 깨진다 — 절대 수정하지 않는다.**

## 3. 데이터 구조 명세

추가되는 데이터는 `FAQItem` 인터페이스를 정확히 따라야 합니다.

```typescript
export interface FAQItem {
    id: string;      // 영문 케밥 케이스(kebab-case). 예: "new-feature-guide"
    question: string; // 사용자의 입장에서 작성된 질문. 예: "새로운 기능은 어떻게 쓰나요?"
    answer: string[]; // 절차별로 나눈 문자열 배열.
}
```

**[작성 예시]**
```typescript
{
    id: "admin-edit-log",
    question: "권한을 가진 관리자는 다른 직원의 운행일지를 수정하거나 대신 작성할 수 있나요?",
    answer: [
        "아니요. 관리자라 하더라도 타인의 운행 기록을 임의로 수정하는 기능은 제공하지 않습니다.",
        "차량운행일지는 국세청 증빙 및 책임 소재를 명확히 하기 위해 **'작성자 본인'만 자신의 일지를 수정**할 수 있도록 설계되어 있습니다.",
        "내용에 오류나 누락이 발견된 경우, 관리자가 임의로 수정하시는 대신 해당 직원(작성자)에게 직접 수정을 요청해 주시기 바랍니다."
    ],
}
```

## 4. 완료 및 배포 확인

파일 수정이 완료되면, 변경된 코드가 안전하게 작동하는지 린팅을 통해 점검하고 필요시 배포 워크플로우를 제안합니다.

1. **로컬 검증**:
   - `npm run lint` 및 타입스크립트 에러 점검
2. **배포 워크플로우 연계**:
   - 배포는 master 푸시 시 CI가 수행하므로 `/deploy` 워크플로우를 사용합니다 (로컬 직접 배포 금지).
