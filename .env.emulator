# Firebase 에뮬레이터 모드 (E2E 인증 테스트 전용)
# vite --mode emulator 로 로드된다. VITE_FIREBASE_* 등 나머지 값은 .env.local에서 병합된다.
# 이 플래그가 켜지면 App Check가 비활성화되고 Auth/Firestore/Functions가 로컬 에뮬레이터에 연결된다.
VITE_USE_EMULATOR=true
