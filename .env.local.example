# === 개발 전용 환경변수 (.env.local) ===
# 이 파일을 .env.local로 복사하여 사용하세요.
# .env.local은 .gitignore에 포함되어 있어 Git에 추적되지 않습니다.

# App Check 디버그 토큰 (Firebase Console → App Check → 디버그 토큰 등록 필요)
VITE_APPCHECK_DEBUG_TOKEN=your-debug-token-here
