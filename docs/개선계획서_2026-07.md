# 개선 계획서 (2026-07)

> 2026-07-04 전 영역 평가(아키텍처·테스트·보안·성능/비용·운영/문서·UX/PWA)에서 도출한 개선 계획.
> 영역별 점수: 아키텍처 9.0 · 성능/비용 9.0 · 보안 8.0 · UX/PWA 8.0 · 운영/문서 7.5 · **테스트 6.0** (종합 8.0/10).
>
> **전제**: 서비스는 완성 단계다. 이 계획은 신규 기능이 아니라 **"만든 것이 맞다는 증명(테스트)"과
> "기록이 현실을 따라가게 하는 것(문서 정합)"** 중심의 마감·수성 작업이다.
>
> **가용 시간**: 평일 1시간/일(급한 것 처리 + 저녁 배포), 주말 2시간(미룬 것 처리) ≈ **주 7시간**.
> 평일 슬롯 = 1시간 이하 조각 작업, 주말 슬롯 = 2시간 묶음 작업으로 배치한다.

---

## 0. 확정된 사실 (2026-07-04 콘솔 실측 — 계획의 근거)

| 항목 | 실측 결과 | 계획 반영 |
|---|---|---|
| App Check 검증률 | Storage **100%** / Firestore **99%** / Auth 72% | Storage·Firestore·Functions 단계 강제, Auth 보류 |
| 인앱 브라우저 사용자 | 활성 사용자의 ≈12% (Android Webview 9.1% + Safari in-app 2.8%). 참여 1분 20초·신규≈활성 → 대부분 랜딩만 보는 일회성 방문 | 로그인 실사용자 차단 위험 낮음 → Functions 강제 가능 |
| `_rateLimits` TTL | `expiresAt` 정책 **제공 중** ✅ | 계획에서 제외 (주석만 갱신) |
| `_health/computeDashboardStats` 3건 | 폐지된 옛 스케줄러의 잔재 (현행 코드는 superAdmin 수동 갱신 구조) | 콘솔에서 문서 삭제만 |

---

## 1주차 — 보안 수정 + 저비용 마감 (평일 슬롯)

| # | 작업 | 예상 | 완료 기준 |
|---|---|---|---|
| P1-1 ✅ | **`cleanupDuplicateLogs` 교차 테넌트 수정** — `functions/src/handlers/callable/cleanupDuplicateLogs.ts:29` 에서 role만 확인하고 호출자 org ↔ `request.data.organizationId` 대조가 없음. claim(`request.auth.token.orgId`) 대조 추가, superAdmin만 타 기관 허용. role도 Firestore 문서 대신 claims로 통일 | 1h | 타 기관 orgId 호출 시 `permission-denied`. **단독 커밋·단독 배포** (`fix:`) — 커밋 `5ea71d8` |
| P1-2 ✅ | P1-1 단위 테스트 — `functions/src/__tests__/cleanupDuplicateLogs.test.ts` 신규 (본인 기관 성공 / 타 기관 거부 / superAdmin 허용 / dryRun 기본값) | 1h | functions `npm test` 통과 — 7건 통과, 커밋 `58e2223` |
| P1-3 ✅ | **토스트 스크린리더 대응** — `src/hooks/ToastProvider.tsx` 컨테이너에 `role="status"` + `aria-live="polite"` (+ `aria-atomic="false"`, 라이브 리전 상시 렌더링) | 0.5h | 커밋 `1227e1d`. 배포 후 실제 스크린리더(TalkBack/NVDA) 낭독 확인 남음 |
| P1-4 ✅ | 콘솔 정리 — `_health`의 `computeDashboardStats*` 문서 3건 삭제 + **Storage App Check 강제**(검증률 100%라 무위험) | 0.5h | **완료 (2026-07-04)**: 콘솔에서 잔재 3건 삭제 + Storage "적용됨". 코드 변경 없음 |

## 1~3주차 — App Check 단계 강제 (관찰 병행, 평일 슬롯)

한 번에 켜지 않는다. **각 단계 후 2~3일 Sentry·Discord 관찰**을 끼운다.

1. **Storage 강제** ✅ 완료 (2026-07-04, P1-4). 이후 2~3일 Sentry·Discord에서 `storage/unauthorized`·업로드/다운로드 `permission-denied` 급증 여부 관찰 중
2. **Firestore 콘솔 강제** — 미검증 1%뿐. Storage 관찰이 조용하면 진행. 강제 후 3일간 `permission-denied` 급증 여부 관찰. 문제 시 콘솔에서 즉시 해제(롤백 코스트 0)
3. **Functions `enforceAppCheck: true` 순차 적용** — 3개 배치로 나눠 배포:
   - 1차: superAdmin 전용 함수(피해 반경 최소 — 본인이 즉시 확인 가능)
   - 2차: 일반 callable (예약·운행일지·사용자 관리)
   - 3차: OCR·askAI (남용 방어 가치가 가장 큰 대상)
   - HTTP 프록시(tmapProxy 등)는 `createAuthenticatedProxy` 구조 확인 후 별도 판단
4. **Auth 강제는 보류** — 프리뷰 단계 + 미검증 28%(인앱 일회성 방문자). Rules와 Functions 강제로 실질 방어 충분
5. `docs/구현이력.md`의 "모든 onCall에 enforceAppCheck 적용" 서술을 실제 진행 상태로 정정

## 2주차 주말 — 문서-코드 드리프트 일괄 청산 (2h, `chore:` 커밋) ✅ 완료 (2026-07-04, 커밋 `5ebd680`)

평가에서 **5개 영역 공통으로 반복된 유일한 주제**. 한 번에 청산했다.

- [x] `OPERATIONS.md` §4.1/§4.2 — 폐지된 `backupFirestore`/`archiveDriveLogs` 개별 함수·시각 → `dailyNightlyBatch`(02:00 KST) 통합 구조로 갱신
- [x] `CONTRIBUTING.md` — `main`→`master`, TailwindCSS v3→v4, `.test.js`→`.test.ts`, 배포 방법(로컬 firebase deploy → CI 단일 경로)
- [x] `.agent/rules/ci-cd.md` — `main`→`master`, "Sentry 소스맵 업로드"를 "현재 미구성"으로 정정(`@sentry/vite-plugin` 미도입 확인)
- [x] `.agent/workflows/release.md` — `main`→`master`
- [x] `docs/FIRESTORE_COST_ANALYSIS.md` — 현행 25개 인덱스·통합 배치(5개 스케줄) 기준으로 갱신, `_rateLimits` TTL "설정 완료(2026-07-04 확인)" 반영
- [x] `functions/src/utils/rateLimit.ts:160` 주석 — TTL 정책 설정 완료로 갱신
- [x] `scripts/check-functions-health.ts` — 폐지 함수명 제거, 현행 export(통합 배치 포함) 기준으로 재작성
- [x] `CHANGELOG.md` — 상단에 "Phase 61 이후 이력은 `docs/구현이력.md`로 대체" 명시하고 종결
- [x] `.agent/agents.md` D17 — Tailwind 정정

> **실제 확인 결과 정정 방향 변경**: 프로젝트는 "v4 엔진 + v3 호환 문법"이 아니라 **완전한 Tailwind v4**였다
> (`index.css`가 `@theme`·`@custom-variant`·`@import "tailwindcss"` 사용, `tailwind.config.js` 없음). 규칙 문서들이
> "v3 문법만, v4 금지"로 정반대로 적혀 있어, **CLAUDE.md·coding-conventions §7·agents.md D17·CONTRIBUTING**을 모두
> v4 기준으로 정정했다. 특히 D17은 "금지 대상"을 반대로 재정의(v3식 설정 신설 금지 → v4 사용).
> **[2026-07-04 확정]** 사용자가 "완전 v4" 방향 확인 → 구현계획서·design-system·index.css의 잔여 v3 표기까지
> 전면 정리(커밋 `b06288d`). 이력 문서(`구현이력.md`)의 과거 Phase 기록은 당시 상태이므로 보존.

## 3~5주차 주말 — 핵심 여정 E2E 구현 (fixme 6건 청산)

에뮬레이터 인증 E2E 인프라(`authed-*` 스펙, `playwright.emulator.config.ts`)는 이미 구축돼 있어
순수 작성 작업만 남았다. 주석에 명시된 기존 단위 테스트를 참조해 여정 검증으로 승격한다.

| 주말 | 대상 | fixme 위치 |
|---|---|---|
| 3주차 (2h) ✅ | 운행일지 작성 + 첨부 검증 (직원 여정) → `e2e/authed-driveLogCreate.spec.ts` 이관·구현, 커밋 `eb087ab` (Authed E2E 5건 통과) | `e2e/core-workflows.spec.ts` |
| 4주차 (2h) ✅ | 관리자 승인/반려 → `e2e/authed-reservationApproval.spec.ts` 이관·구현, 커밋 `5e288f5` (Authed E2E 7건 통과). **E2E가 실제 버그 검출**: 전역 ConfirmModal이 `type=input`을 렌더 안 함(반려 사유 입력창 누락) → 수정 커밋 `b2f1dca`. 예약 "생성"은 `createReservationSafe` 콜러블이라 functions 에뮬레이터 없는 환경에서 UI 재현 불가(단위 테스트 커버) | `e2e/core-workflows.spec.ts` + `e2e/reservation-approval.spec.ts` (2건) |
| 5주차 (2h) ✅ | 내보내기 → `e2e/authed-driveLogExport.spec.ts` 이관·구현(엑셀 다운로드), 커밋 `07d4bd2` (CI 조건 8건 통과). **도구 제약**: PDF는 `window.open`+`print`(인쇄 대화상자)라 헤드리스 자동화 불가 → 엑셀만 E2E. **403 토스트는 미구현 확정**: 앱이 permission-denied를 재시도+폴백으로 복구하는 방어적 설계라 정상 여정으로 재현 불가(근거를 `error-recovery.spec.ts`에 문서화, 권한 경계는 smoke 리다이렉트·rules 테스트가 커버) | `e2e/core-workflows.spec.ts` + `e2e/error-recovery.spec.ts` |

완료 기준: E2E 가능한 여정(운행일지 작성·예약 승인/반려·내보내기)은 모두 `authed-*` 스펙으로 승격 완료. E2E 불가 fixme 2건(예약 생성=functions 콜러블, 403 토스트=방어적 복구 설계)은 근거를 문서화하고 단위/rules 테스트로 커버.

> **E2E 트랙 결산(3~5주차)**: authed E2E 스펙 = smoke·offline + driveLogCreate·reservationApproval·driveLogExport, CI 게이트에서 8건 실행. E2E가 실제 버그 1건 검출·수정(ConfirmModal 입력, `b2f1dca`) + 스모크 플레이키 1건 견고화. 도구·설계상 E2E 불가한 fixme는 "왜 불가한지"를 스펙 파일에 남겨 정직화.

## 6주차~ — 지속 트랙 (주 1건 리듬)

한 번에 몰지 않고 평일 슬롯 1건/주로 유지한다.

1. **`src/lib/firestore/` 도메인 테스트** — 주 1파일. 저위험 CRUD 5파일 완료 ✅ (2026-07-06, Phase 87: vehicles·users·favorites·holidays·fuelLogs 49건 + mock 하네스 확립). 잔여: `reservations.ts`(상태 전이·동시성)·`driveLogs/mutations.ts`(오프라인 큐 분기) — mock 복잡도가 높아 신중 트랙(계획서 선행)
2. **커버리지 임계치 단계 상향** — `vitest.config.js` lines 24 → 28 → 32 (테스트 추가에 맞춰; 목표는 숫자가 아니라 후퇴 방지). 1단계 완료 ✅ (2026-07-06): Phase 87 실측(lines 26.57) 기준 26/25/20/15로 잠금, 28은 reservations·driveLogs 테스트 추가 후
3. **접근성 마감** — `prefers-reduced-motion` 미디어쿼리(`src/index.css`) ✅ (2026-07-06, Phase 88 — 스피너는 상태 전달 수단이라 예외 유지). ~~Zod 검증 실패 시 필드·사유 노출~~ **전제 무효로 종결** (2026-07-06 확인): `driveLogSchema`·`reservationSchema` 전 필드가 `.catch()` 자가 치유라 `parse()`가 ZodError를 던질 수 없고, 사용자 대면 검증은 `validateDriveLogForm`이 이미 한국어 메시지 제공. `useDriveLogSubmit`의 "알 수 없는 오류"는 Zod가 아니라 예상 밖 에러용 최종 방어선으로 정상
4. **react-hot-toast 잔재 제거** — `<Toaster>` 미마운트 상태의 죽은 `toast()` 호출 2개 파일을 커스텀 토스트로 통일, 패키지 제거. 완료 ✅ (2026-07-06, Phase 90 — lightEntry에 ToastProviderWrapper 마운트로 랜딩 문의 폼 토스트 사각지대까지 해소, 번들 -8.4KB)
5. **릴리즈 태그 관행 시작** — 의미 있는 배포마다 `git tag`(롤백 앵커). `release.md` 워크플로 활용. 시작 ✅ (2026-07-06): 첫 태그 `v1.0.0`을 Phase 90 배포 커밋(`5b97c31`)에 부여(package.json 버전과 일치, 버전 범프 없이 현행 프로덕션 앵커). 이후 의미 있는 배포부터 `/release`로 `npm version` 범프 흐름

## 하지 않기로 한 것 (근거와 함께)

| 항목 | 이유 |
|---|---|
| Auth App Check 강제 | 프리뷰 단계 + 미검증 28%. Rules·Functions 강제로 충분 |
| Secret Manager(`defineSecret`) 전환 | 현 `functions/.env` 방식도 노출 없음. 1인 운영 규모에서 회전·감사 요구 낮음 — 여유 시 |
| OCR 콘텐츠 해시 캐시 | 일일 쿼터(20/일·50/일)로 비용 상한 확보됨. 실제 비용 문제 발생 시 |
| 대시보드 차트 5,000문서 상한 개선 | 현 기관 규모에서 미도달. 성장 시 시계열 사전 집계로 흡수 |
| 개별 청크 번들 예산 | 전체 합산 예산(3MB, CI 게이트)으로 충분. 회귀 발생 시 도입 |
| Cloud Monitoring IaC화 | 수동 설정으로 충분한 규모 |

## 진행 규칙

- 배포는 CI 단일 경로(master 푸시). 로컬 `firebase deploy` 금지.
- 검증은 Node 22: `fnm exec --using=22 npm.cmd run <script>`.
- P1-1(보안 수정)은 다른 변경과 섞지 않고 단독 커밋·단독 배포.
- 문서 정리는 `chore:`, 보안 수정은 `fix:`, 테스트 추가는 `test:` 커밋으로 분리.
- 각 주차 완료 시 `docs/구현이력.md` 트랙 B에 Phase 추가 + 구현계획서 §17 포인터 갱신.
- 예상 총 소요: **6~8주** (주 7시간 기준, App Check 관찰 기간 포함).
