# 2026-07-10 코덱스 평가 대응 개선 계획

> **에이전트 작업자용:** 구현 시 아래 트랙을 순서대로 수행한다. 모든 실행 단계는 체크박스로 추적하며, 트랙별로 독립 커밋·독립 검증·독립 롤백이 가능해야 한다.

- 기준 평가: [docs/2026-07-10코덱스평가.md](2026-07-10코덱스평가.md) — Codex, **84/100 (A-)**
- 목표: 코덱스 지적 중 유효 항목을 해소해 재평가 **90점 이상** 달성
- 작성일: 2026-07-11

## 기존 계획서와의 관계

이 문서는 [docs/20260710개선계획서.md](20260710개선계획서.md)(81점 평가 기반, Phase 93·94로 대부분 반영)의 **후속 계획**이다. 코덱스 84점 평가는 Phase 93(보안 감사 조치)·Phase 94(품질 게이트 티어링) 코드가 반영된 시점의 저장소를 대상으로 했다.

**선행 의존(이 계획 시작 전 완료 필수):**

- [ ] Phase 93·94 코드의 프로덕션 배포 (master 푸시 → CI Deploy)
- [ ] OAuth 토큰 마이그레이션 dry-run → 실행 → verify (`remaining=0`) — 기존 계획서 작업 1의 8~10단계
- [ ] App Check 1배치(운영 함수 8개) 배포 후 2영업일 관찰 통과

## 코덱스 지적사항 삼분류

### ✅ 이미 해결됨 (조치 불필요 — 재평가 시 이견 근거)

| 코덱스 지적 | 현재 상태 | 근거 |
|---|---|---|
| gzip 번들 예산 부재 | raw 3,000KB / gzip 950KB / 최대 청크 420KB / CSS 200KB·35KB 하드 게이트 존재, CI 실행 | `scripts/check-bundle-size.ts:18-24`, `.github/workflows/ci.yml` |
| 접근성 자동검증 | axe serious/critical 하드 게이트 존재, /terms·/privacy 포함 5개 경로 검사 | `e2e/accessibility-axe.spec.ts:17,36-56` |
| 인증 E2E | 시드 계약·단일 워커로 안정화 (Phase 94, 3회 연속 8/8) | `playwright.emulator.config.ts`, `e2e/emulator/seed.ts` |
| CI 워크플로 권한 | `permissions: contents: read` 명시 | `ci.yml:16-17`, `deploy.yml:14-15` |

### 🔄 진행 중 (기존 계획 승계)

| 코덱스 지적 | 현재 상태 | 근거 |
|---|---|---|
| App Check 미강제 19곳 | 8/27 함수 `true`(1배치 완료). 일반 인증 13개·AI/OCR 3개·공개 2개 `false` 잔존 | `functions/src/handlers/callable/*.ts` |
| 프론트 커버리지 낮음 (라인 29.13%) | 게이트 lines 28 / statements 27 / functions 21 / branches 17 — 기존 계획 최종 목표(35/34/28/25) 미달 | `vitest.config.js:39-44` |

### 🆕 신규 지적 (이 계획의 본체)

| 코덱스 지적 | 현재 상태 | 근거 |
|---|---|---|
| Rate Limit fail-open | Firestore 장애 시 의도적 통과. OCR 일일 쿼터(`checkDailyOcrQuota`)도 동일 fail-open 경로 재사용 | `functions/src/utils/rateLimit.ts:69-74,150-154` |
| 기관 신청 MIME 검증 부재 | 존재 확인만 수행, 캘러 제공 MIME을 그대로 `contentType`으로 저장 | `functions/src/handlers/https/submitOrgApplication.ts:38-40,86-97` |
| Functions 커버리지 미강제 | jest `coverageThreshold` 없음, CI는 테스트 실행만 | `functions/package.json`, `ci.yml:71` |
| 색상 대비 부채 4건 | `color-contrast`가 `REPORT_ONLY_RULES`로 게이트 제외 (예: 3.08:1) | `e2e/accessibility-axe.spec.ts:11-20` |
| CSP `unsafe-inline` | script-src·style-src 모두 포함 | `firebase.json:56` |
| firebase-tools 버전 미고정 | devDependencies에 없음, CI·deploy가 `npx firebase-tools`로 최신 해석 | `ci.yml:90`, `deploy.yml:71` |
| 400줄 이상 대형 파일 5개 | 559/503/458/417/415줄, 변동 없음 | 작업 9 참조 |
| 문서 드리프트 | README "ESM" 오기(실제 CommonJS), 테스트 수치 낡음, PROJECT.md 클레임 키 불일치 | 작업 11 참조 |

## 의도적 미채택 재확인

코덱스가 재지적했으나 Phase 94에서 사유와 함께 미채택한 항목은 **결정을 유지**한다 ([docs/구현이력.md](구현이력.md) Phase 94 기록 참조).

| 항목 | 결정 | 사유 | 재검토 조건 |
|---|---|---|---|
| deploy.yml 별도 verify job / Environment 승인 | 미채택 유지 | deploy job이 build·vitest·audit를 재실행하고 `workflow_run` CI 성공 조건에 의존. 1인 운영에서 승인 단계는 긴급 배포를 지연시킴 | 운영 인원 2인 이상 또는 수동 배포 사고 1회 발생 시. 단, 경량 보강(작업 7의 lint·type-check 추가)은 이번에 채택 |
| prebuild `--strict` Node 강제 | 미채택 유지 | engines 22 + CI Node 22 고정으로 배포 재현성 확보됨. 로컬은 `fnm exec --using=22` 관행 | Node 24 빌드 산출물로 인한 실제 결함 1회 발생 시 |

## 전역 제약

- 모든 검증과 빌드는 Node.js 22.x에서 실행한다 (`fnm exec --using=22 npm.cmd ...`).
- 프로덕션 배포는 master 푸시 → CI Deploy 경로만 사용한다. 로컬 `firebase deploy` 금지.
- 푸시는 작업(커밋 묶음) 단위로 모아 1회 수행한다 (Actions 분량 관리).
- 보안 변경, 테스트 변경, 문서 변경은 서로 다른 커밋으로 분리한다.
- App Check 배치 적용 후 각 2영업일 관찰을 거친 뒤 다음 배치로 진행한다.
- 커버리지 하한은 실측값보다 최소 1%p 낮게 설정해 비결정적 변동을 흡수한다.
- 신규 의존성 추가는 `firebase-tools`(devDependency 고정) 한 개로 제한한다.

## 일정 요약 (주 7시간 기준, 약 6주)

| 시기 | 트랙 | 작업 | 완료 게이트 |
|---|---|---|---|
| 1주차 | P0 | 작업 1(App Check 2배치) + 작업 3(MIME 화이트리스트) | 2배치 배포·관찰 개시, MIME 거부 테스트 통과 |
| 2주차 | P0 | 작업 1(App Check 3배치) + 작업 2(Rate Limit fail-closed) | 인증 callable 24/24 강제, failMode 단위 테스트 통과 |
| 3주차 | P1 | 작업 4(Functions 커버리지 게이트) + 작업 6(색상 대비) | CI에서 Functions 임계치 강제, color-contrast 게이트 편입 |
| 4주차 | P1 | 작업 5(프론트 커버리지 상향) | 라인 35 / 브랜치 25 게이트 통과 |
| 5주차 | P2 | 작업 7(배포 재현성) + 작업 11(문서 정합성) + 작업 8 착수 | firebase-tools 고정, 문서 불일치 0건 |
| 6주차~ | P2 | 작업 8(번들 여유) + 작업 9(대형 파일, 주 1개 페이스) + 작업 10(CSP 실험) | 소프트 목표 — 결과 기록으로 종료 가능 |

---

## P0 트랙 — 보안 (1~2주차)

### 작업 1: App Check 2·3배치 강제

기존 계획서 작업 4의 3~5단계를 그대로 승계한다.

**파일:** `functions/src/handlers/callable/*.ts` (아래 16개)

- [ ] **1단계: 2배치 — 일반 인증 함수 13개에 `enforceAppCheck: true` 적용.**

대상: `createReservationSafe`, `joinOrganization`, `disableUser`, `restoreUser`, `sendAdminNotice`, `sendApprovalEmail`, `sendRejectionEmail`, `sendFeedbackReply`, `sendManualApprovalAlimtalk`, `sendManualRejectionAlimtalk`, `triggerOnDemandCalendarSync`, `testCalendarAccess`, `withdrawOrganization`

```powershell
fnm exec --using=22 npm.cmd run type-check:functions
fnm exec --using=22 npm.cmd --prefix functions test -- --maxWorkers=2 --workerIdleMemoryLimit=512MB
git commit -am "security: 인증 업무 함수 App Check 강제 (2배치)"
```

- [ ] **2단계: 2배치 배포 후 2영업일 관찰.**

정상 기준: App Check invalid/expired 비율 1% 미만, callable `unauthenticated`·`permission-denied` 오류율이 직전 7일 평균 대비 20% 이상 증가하지 않음. 실패 사용자의 User-Agent가 인앱 브라우저인지 별도 집계. 브라우저에서 예약 생성/취소, 직원 비활성화/복원, 피드백 답변, 캘린더 수동 동기화 각 1회 실검증.

- [ ] **3단계: 3배치 — AI·OCR 함수 3개 적용.**

대상: `ocrDashboard`, `ocrDocument`, `askAI`. OCR 일일 제한과 기존 rate limit은 그대로 유지.

검증: 계기판 OCR, 기관 문서 OCR, AI 도움말 각 3회 실행 성공률 100%.

```powershell
git commit -am "security: AI OCR 함수 App Check 강제 (3배치)"
```

- [ ] **4단계: 공개 callable 예외 재확인.**

`submitOrgApplication`, `submitPublicFeedback`은 비로그인 진입 특성상 `false` 유지 (기존 예외 문서·2026-08-10 재검토일 유효). `scripts/resetCalendarSyncFails.ts`는 배포 대상이 아니므로 제외.

**롤백:** 오류율 20% 이상 증가 시 해당 배치만 `false`로 복원해 단독 재배포.

### 작업 2: Rate Limit 고위험 API fail-closed

**파일:**

- 수정: `functions/src/utils/rateLimit.ts`
- 수정: OCR·AI·공개 접수 호출부 (`ocrDashboard`, `ocrDocument`, `askAI`, `submitOrgApplication`, `submitPublicFeedback`)
- 생성: `functions/src/__tests__/rateLimitFailMode.test.ts`

**인터페이스:**

- `failMode?: 'open' | 'closed'` 옵션 추가. **기본값 `'open'`** — 기존 모든 호출부의 동작을 보존한다.
- `failMode: 'closed'`에서 Firestore 접근 실패 시 요청을 거부한다 (callable은 `resource-exhausted`, HTTPS는 429).

- [x] **1단계: 실패 계약 테스트를 먼저 작성한다.** ✅ 2026-07-11 `rateLimitFailMode.test.ts`

Firestore 트랜잭션 mock을 reject시켜 `open`은 통과, `closed`는 거부하는 두 계약을 단언한다. `checkDailyOcrQuota`가 `closed` 경로를 타는지도 검증한다.

- [x] **2단계: `rateLimit.ts`의 uid·IP 두 catch 블록(현 `:69-74`, `:150-154`)에 failMode 분기를 구현한다.** ✅

- [x] **3단계: 고위험 호출부 5곳에만 `failMode: 'closed'`를 지정한다.** 일반 조회·업무 API는 fail-open 유지 (가용성 우선). ✅ `wrapCallableHandler`에 `rateLimitFailMode` 옵션 추가로 전달

- [x] **4단계: 검증·커밋.** ✅ 커밋 `27e2b21` (commitlint가 `security:` 타입을 불허해 `fix:` 사용)

```powershell
fnm exec --using=22 npm.cmd run type-check:functions
fnm exec --using=22 npm.cmd --prefix functions test -- --maxWorkers=2 --workerIdleMemoryLimit=512MB
git commit -am "security: OCR·AI·공개 접수 Rate Limit fail-closed 전환"
```

**롤백:** 오탐(정상 사용자 차단) 발생 시 해당 호출부의 `failMode`만 제거 — 기본값 open으로 즉시 복원된다.

### 작업 3: 기관 신청 MIME 화이트리스트

**파일:**

- 수정: `functions/src/handlers/https/submitOrgApplication.ts`
- 수정: 해당 핸들러 테스트 (`functions/src/__tests__/`)

- [x] **1단계: 거부 테스트를 먼저 작성한다.** `text/html`, `application/octet-stream`, 빈 문자열 → `invalid-argument` 거부. `image/jpeg`, `image/png`, `image/webp`, `application/pdf` → 통과. ✅ 2026-07-11 `submitOrgApplication.test.ts`

- [x] **2단계: 화이트리스트 상수를 정의하고 `imageMimeType` 존재 확인(현 `:38-40`) 직후에 검증을 추가한다.** ✅ 확장자·contentType 정합 매핑 포함 (png가 .jpg로 저장되던 것도 해소)

```ts
const ALLOWED_MIME_TYPES = new Set([
  "image/jpeg",
  "image/png",
  "image/webp",
  "application/pdf",
]);
```

매직 바이트 검증까지는 이 프로젝트 규모에서 과잉이다 — 5MB 크기 상한·rate limit·Storage Rules가 병존한다.

- [x] **3단계: 검증·커밋.** ✅ 커밋 `27e2b21` (Functions 31 suites/284 통과)

```powershell
fnm exec --using=22 npm.cmd --prefix functions test -- --maxWorkers=2 --workerIdleMemoryLimit=512MB
git commit -am "security: 기관 신청 업로드 MIME 화이트리스트 검증 추가"
```

---

## P1 트랙 — 테스트·접근성 (3~4주차)

### 작업 4: Functions 커버리지 게이트 신설

**파일:** `functions/package.json`, `.github/workflows/ci.yml`

- [ ] **1단계: 현재 커버리지를 실측한다.**

```powershell
fnm exec --using=22 npm.cmd --prefix functions test -- --coverage --maxWorkers=2 --workerIdleMemoryLimit=512MB
```

- [ ] **2단계: 실측값 −1~2%p로 `coverageThreshold`(global)를 jest 설정에 추가한다.** 회귀 방지선이 목적이므로 처음부터 높게 잡지 않는다.

- [ ] **3단계: CI의 Functions 테스트 스텝(`ci.yml:71`)에 `--coverage`를 추가한다.**

- [ ] **4단계: 우선 보강 — 코덱스 지적 순서대로 예외 경로 테스트를 추가한다.**

우선순위: ① 인증·권한 거부 경로 ② OCR·AI 등 비용 큰 경로의 실패 처리 ③ 조직 삭제·복원 ④ 스케줄 배치의 부분 실패·멱등성. 추가 후 임계치를 실측 −1%p로 재상향한다.

- [ ] **5단계: 커밋.**

```powershell
git add functions/package.json .github/workflows/ci.yml functions/src/__tests__
git commit -m "test: Functions 커버리지 게이트 신설 및 예외 경로 보강"
```

**롤백:** CI 비결정 실패가 반복되면 임계치만 1%p 하향 (게이트 자체 제거 금지).

### 작업 5: 프론트 커버리지 상향 (기존 계획 작업 3 후반부 승계)

**파일:** `src/__tests__/` 신규 테스트, `vitest.config.js`

현재 게이트: lines 28 / statements 27 / functions 21 / branches 17.

- [ ] **1단계: 1차 상향 — 실측이 33/32/26/21 이상일 때 게이트를 `32/31/25/20`으로 상향.**

- [ ] **2단계: 우선 대상 테스트 추가.** 코덱스 지적 경로: 예약 편집·취소, 운행일지 제출, PDF·Excel 생성, 관리자 권한 변경, 외부 API 실패 경로. 기존 계획의 저커버리지 Firestore 모듈(`maintenance.ts`, `notifications.ts`, `dailyLogQueries.ts`)도 포함.

- [ ] **3단계: 최종 게이트 `35/34/28/25` 적용.** (코덱스의 라인 40% 제안은 스트레치 목표로만 두고 게이트로 강제하지 않는다 — 주 7h 가용 시간 기준 4주 내 40%는 무리한 페이스다.)

- [ ] **4단계: 검증·커밋.**

```powershell
fnm exec --using=22 npm.cmd run test:coverage
git add src/__tests__ vitest.config.js
git commit -m "test: 프론트 커버리지 게이트 35/25 상향"
```

**롤백:** 임계치 하향 금지 — 미달 시 부족한 분기 테스트를 추가한다.

### 작업 6: 색상 대비 4건 수정 + axe 게이트 편입

**파일:**

- 수정: /terms·/privacy 렌더 컴포넌트 (위반 노드 기준으로 특정)
- 수정: `e2e/accessibility-axe.spec.ts`

- [ ] **1단계: 위반 목록을 실측한다.** axe 리포트에서 color-contrast 위반 노드 4건(이용약관 2·개인정보 2)의 셀렉터와 대비값을 확보한다.

- [ ] **2단계: 대비 4.5:1 이상으로 수정한다.** 예: `text-primary-300/60 on bg-primary-900`(3.08:1) → 투명도 제거 또는 단계 조정. 라이트·다크 모드 양쪽에서 확인한다 ([dark-mode-audit](.agent/skills/dark-mode-audit/SKILL.md) 참조). lightEntry(비로그인) 페이지이므로 비로그인 화면에서 검증한다.

- [ ] **3단계: `REPORT_ONLY_RULES`에서 `color-contrast`를 제거해 하드 게이트에 편입한다** (`accessibility-axe.spec.ts:19-20`, 사유 주석 `:11-15`도 함께 정리).

- [ ] **4단계: 검증·커밋.**

```powershell
npx playwright test e2e/accessibility-axe.spec.ts
git commit -am "fix: 약관·개인정보 페이지 색상 대비 수정 및 axe 게이트 편입"
```

**롤백:** 편입 후 새 화면에서 위반이 반복 유입되면 해당 룰을 다시 REPORT_ONLY로 내리는 대신, 위반 노드를 수정한다. 게이트 재제외는 디자인 시스템 차원의 팔레트 문제로 확인된 경우에만 허용하고 사유를 주석으로 남긴다.

---

## P2 트랙 — 성능·구조·운영 (5주차~)

### 작업 7: 배포 재현성

**파일:** `package.json`, `package-lock.json`, `.github/workflows/deploy.yml`

- [ ] **1단계: `firebase-tools`를 devDependencies에 버전 고정으로 추가한다.** CI(`ci.yml:90`)·deploy(`deploy.yml:71`)의 `npx firebase-tools`가 lock 파일의 고정 버전을 해석하는지 확인한다.

- [ ] **2단계: deploy.yml 수동 경로(workflow_dispatch)에 lint·type-check 스텝을 추가한다.** 이미 build·vitest·audit는 재실행하므로 이 두 개만 보강한다. 별도 verify job·Environment 승인은 신설하지 않는다 (의도적 미채택 유지).

- [ ] **3단계: 커밋.**

```powershell
git add package.json package-lock.json .github/workflows/deploy.yml
git commit -m "ci: firebase-tools 버전 고정 및 수동 배포 정적 검사 보강"
```

### 작업 8: 번들 예산 여유 15% 확보 (소프트 목표)

**목표:** raw ≤ 2,550KB, gzip ≤ 807KB (예산의 85%). **하드 게이트(3,000/950KB)는 변경하지 않는다.** 달성 실패 시 분석 결과를 기록하고 종료할 수 있다.

- [ ] **1단계: 청크 분석.** `/perf` 워크플로우 또는 Vite 빌드 출력으로 recharts·firebase-db·xlsx 청크의 크기와 로딩 유발 라우트를 확인한다.

- [ ] **2단계: 라우트별 로딩 시점 점검.** 초기 진입 그래프에 통계·내보내기 전용 청크가 포함되지 않는지 확인하고, 포함 시 지연 로딩으로 전환한다.

- [ ] **3단계: 부족 시 경량 대체 검토.** recharts 부분 import 최적화, xlsx 대체 빌드 등 — 각각 별도 판단이 필요한 작업이므로 이 계획에서는 후보 목록과 예상 절감량 기록까지만 수행한다.

- [ ] **4단계: 결과를 `docs/구현이력.md`에 기록한다** (달성/미달성 무관).

### 작업 9: 대형 파일 5개 분리 (주 1개 페이스)

분리 기준: 통계 계산 / Firestore 입출력 / 응답 조립 / React 렌더링. 파일당 독립 커밋 + 기존 테스트 통과 확인. **동작 변경 없는 순수 이동만** 수행한다.

- [ ] `functions/src/services/statistics/dashboardSections.ts` (559줄) — 섹션별 계산 함수 분리
- [ ] `src/hooks/utils/analyticsCalc.ts` (503줄) — 도메인별 계산 모듈 분리
- [ ] `functions/src/handlers/https/apiHealthCheck.ts` (458줄) — 체크 항목별 함수 분리
- [ ] `functions/src/handlers/triggers/reservationTriggers.ts` (417줄) — 트리거별 핸들러 분리
- [ ] `src/components/superAdmin/OrgManagement.tsx` (415줄) — 하위 컴포넌트·훅 추출

각 분리 후:

```powershell
fnm exec --using=22 npm.cmd run lint
fnm exec --using=22 npm.cmd run type-check
fnm exec --using=22 npm.cmd run type-check:functions
fnm exec --using=22 npm.cmd test
```

**중단 기준:** 분리 과정에서 동작 변경이 필요해지면 해당 파일은 중단하고 사유를 기록한다 (리팩토링에 기능 수정을 끼워 넣지 않는다).

### 작업 10: CSP 단계 강화 (실험 항목)

**파일:** `firebase.json`

- [ ] **1단계: `Content-Security-Policy-Report-Only` 헤더로 script-src에서 `unsafe-inline` 제거안을 관찰 모드로 배포한다.** 기존 CSP는 유지한 채 위반 리포트만 수집한다.

- [ ] **2단계: 1~2주 관찰 후 위반이 없으면 script-src에서만 `unsafe-inline`을 제거한다.** style-src는 Recharts·Leaflet 인라인 스타일 의존으로 유지한다.

- [ ] **3단계: 위반이 확인되면 원인(GA·Firebase 스니펫 등)을 기록하고 중단한다.** 이 항목은 중단해도 계획 완료에 영향을 주지 않는다.

### 작업 11: 문서 정합성

**파일:** `README.md`, `PROJECT.md`

- [ ] **1단계: README Functions 모듈 형식 교정.** `README.md:38,182`의 "TypeScript ESM" → "TypeScript CommonJS" (실제 `functions/tsconfig.json:4`는 `"module": "commonjs"`). ※ CLAUDE.md에도 "Cloud Functions는 ESM" 표기가 있으므로 함께 교정하되, `.agent/` 원본 규칙 파일에 동일 표기가 있는지 확인 후 원본부터 수정하고 `npm run sync:agents`로 재생성한다.

- [ ] **2단계: README 테스트 수치를 고정 숫자 대신 "실행 명령 + 최소 기준" 표기로 전환한다** (`README.md:246-249`, 현재 49파일/357개 → 실측 74파일). 코덱스 권고대로 변동 잦은 수치는 정확한 개수를 적지 않는다.

- [ ] **3단계: PROJECT.md 클레임 키 교정.** `PROJECT.md:29`의 `request.auth.token.organizationId` → `orgId` (실제 클레임: `functions/src/handlers/triggers/setCustomClaims.ts:32,46`). Firestore 문서 필드는 `organizationId`, 토큰 클레임은 `orgId`라는 구분을 한 줄로 명시한다.

- [ ] **4단계: E2E spec 수 등 잔여 수치 확인 후 `docs:` 커밋으로 분리.**

```powershell
rg -n "TypeScript ESM|49파일|357개|19개 suite|172개|organizationId" README.md PROJECT.md CLAUDE.md
npm run sync:agents -- --check
git add README.md PROJECT.md CLAUDE.md .agent
git commit -m "docs: Functions 모듈 형식·테스트 수치·클레임 키 문서 정합성 교정"
```

---

## 중단·롤백 기준

| 상황 | 중단 기준 | 조치 |
|---|---|---|
| App Check 2·3배치 | 오류율 20% 이상 증가 또는 정상 사용자 차단 | 해당 배치만 `false` 복원 후 단독 배포 |
| Rate Limit fail-closed | 정상 사용자 오탐 차단 | 해당 호출부 `failMode` 제거 (기본 open 복원) |
| MIME 화이트리스트 | 정상 신청 거부 사례 | 누락 MIME을 화이트리스트에 추가 (검증 제거 금지) |
| 커버리지 게이트 | 비결정 실패 반복 | 임계치 1%p 하향만 허용, 게이트 제거 금지 |
| 색상 대비 게이트 | 디자인 팔레트 차원 위반 유입 | 위반 노드 수정 우선, 재제외는 사유 주석 필수 |
| 대형 파일 분리 | 동작 변경 필요 판명 | 해당 파일 중단·사유 기록 |
| CSP 실험 | Report-Only에서 위반 확인 | 원인 기록 후 중단 (계획 완료에 무영향) |

## 완료 정의

- [ ] 인증 callable App Check 적용률 100% (24/24, 공개 2건은 문서화된 예외)
- [x] OCR·AI·공개 접수 5개 경로 Rate Limit fail-closed + 단위 테스트 통과 (2026-07-11, `27e2b21`)
- [x] 기관 신청 MIME 화이트리스트 검증 + 거부 테스트 통과 (2026-07-11, `27e2b21`)
- [ ] Functions 커버리지 임계치가 CI에서 강제됨
- [ ] 프론트 커버리지 게이트 lines 35 / branches 25 통과
- [ ] `color-contrast`가 axe 하드 게이트에 편입되고 위반 0건
- [ ] firebase-tools 버전 고정, 수동 배포 경로에 lint·type-check 포함
- [ ] README·PROJECT.md·CLAUDE.md의 모듈 형식·수치·클레임 키 불일치 0건
- [ ] 번들·대형 파일·CSP는 결과 기록 완료 (달성 여부 무관)
- [ ] 재평가 90점 이상

## 재평가 시 이견 기록 (다음 평가자 참고)

- 코덱스 보고서의 "번들 예산 여유 부족" 지적 중 gzip 게이트 부재 부분은 평가 시점에 이미 `scripts/check-bundle-size.ts`에 반영되어 있었다 (Phase 94).
- "수동 배포 승인 부재"와 "Node strict 미적용"은 Phase 94에서 사유와 함께 의도적으로 미채택한 결정이다 (위 재확인 절 참조). 1인 운영·주 7h 가용 시간 규모에서 비용 대비 효익이 낮다고 판단했다.
- `enforceAppCheck: false` 19곳 중 1곳(`scripts/resetCalendarSyncFails.ts`)은 배포되지 않는 일회성 스크립트다.
